||0;b.files+=c.stats.total_files||0;b.known++;rr.add+=c.stats.additions||0;rr.del+=c.stats.deletions||0}}S.buckets=[...map.values()]}
function streak(list){const days=[...new Set(list.map(c=>dateOf(c).toISOString().slice(0,10)))].sort();let best=0,run=0,prev=0;for(const s of days){const t=+new Date(s+"T00:00:00Z");run=prev&&t-prev===DAY?run+1:1;best=Math.max(best,run);prev=t}return best}
function render(){makeBuckets();document.querySelectorAll("[data-range]").forEach(b=>b.classList.toggle("active",b.dataset.range===S.range));document.querySelectorAll("[data-mode]").forEach(b=>b.classList.toggle("active",b.dataset.mode===S.mode));const exp={lines:"Green is lines added. Red is lines removed. Tap a period for repository details.",commits:"Each green bar is the number of commits in that period.",repos:"Each amber bar is the number of distinct repositories changed in that period."};E.explain.textContent=exp[S.mode];E.cap.textContent={lines:"Lines added and removed",commits:"Commit count",repos:"Repositories changed"}[S.mode];E.legend.textContent={lines:"green = added / red = removed",commits:"green = commits",repos:"amber = repositories"}[S.mode];const list=filtered(),repos=new Map;let add=0,del=0,known=0;for(const c of list){repos.set(repoOf(c),(repos.get(repoOf(c))||0)+1);if(c.stats){add+=c.stats.additions||0;del+=c.stats.deletions||0;known++}}$("mCommits").textContent=fmt(list.length);$("mRepos").textContent=fmt(repos.size);$("mAdd").textContent=known?fmt(add):"-";$("mDel").textContent=known?fmt(del):"-";$("mDays").textContent=fmt(new Set(list.map(c=>dateOf(c).toISOString().slice(0,10))).size);$("mStreak").textContent=fmt(streak(list));E.summary.textContent=`${fmt(list.length)} commits across ${fmt(repos.size)} repositories in ${S.range}`;E.notice.textContent=known<list.length?`${fmt(known)} of ${fmt(list.length)} commits have exact line data`:"Exact line data loaded";$("recentRange").textContent=S.range;E.commits.innerHTML=list.slice(0,12).map(c=>`<li class="row"><a href="${esc(c.html_url)}" target="_blank" rel="noopener"><div class="msg">${esc((c.commit?.message||"Commit").split("\n")[0])}</div><div class="sub"><span>${esc(repoOf(c))}</span><span>${esc(c.sha.slice(0,7))}</span><span>${dateOf(c).toLocaleDateString()}</span></div></a></li>`).join("")||'<li class="row">No commits in this range.</li>';const top=[...repos].sort((a,b)=>b[1]-a[1]).slice(0,14),mx=top[0]?.[1]||1;E.repoList.innerHTML=top.map(([r,n])=>`<li class="row"><div class="repotop"><b>${esc(r)}</b><span>${fmt(n)}</span></div><div class="bar"><i style="width:${n/mx*100}%"></i></div></li>`).join("")||'<li class="row">No repositories.</li>';resize()}
function resize(){const r=E.chart.getBoundingClientRect(),d=Math.min(devicePixelRatio||1,2);E.canvas.width=Math.max(1,Math.round(r.width*d));E.canvas.height=Math.max(1,Math.round(r.height*d));C.setTransform(d,0,0,d,0,0);draw()}
function draw(){const w=E.canvas.clientWidth,h=E.canvas.clientHeight,p={l:45,r:18,t:54,b:38},cw=w-p.l-p.r,ch=h-p.t-p.b;C.fillStyle="#101214";C.fillRect(0,0,w,h);C.strokeStyle="rgba(255,255,255,.09)";C.lineWidth=1;for(let i=0;i<5;i++){const y=p.t+i*ch/4;C.beginPath();C.moveTo(p.l,y);C.lineTo(w-p.r,y);C.stroke()}if(!S.buckets.length)return;const vals=S.buckets.map(b=>S.mode==="commits"?b.commits.length:S.mode==="repos"?b.repos.size:Math.max(b.add,b.del)),max=Math.max(1,...vals),slot=cw/S.buckets.length,bw=Math.max(3,Math.min(18,slot*.55));S.buckets.forEach((b,i)=>{const x=p.l+i*slot+(slot-bw)/2;if(S.mode==="