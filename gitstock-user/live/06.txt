lines"){const mid=p.t+ch*.48,ah=(b.add/max)*ch*.42,dh=(b.del/max)*ch*.42;C.fillStyle="#22bd83";C.fillRect(x,mid-ah,bw,ah);C.fillStyle="#ec6963";C.fillRect(x,mid,bw,dh);if(!b.known){C.strokeStyle="rgba(255,255,255,.3)";C.strokeRect(x,mid-2,bw,4)}}else{const v=S.mode==="commits"?b.commits.length:b.repos.size,bh=v/max*ch*.86;C.fillStyle=S.mode==="commits"?"#22bd83":"#d99b2b";C.fillRect(x,p.t+ch-bh,bw,bh)}});C.fillStyle="rgba(255,255,255,.42)";C.font="10px system-ui";C.textAlign="center";const every=Math.max(1,Math.ceil(S.buckets.length/5));S.buckets.forEach((b,i)=>{if(i%every===0||i===S.buckets.length-1)C.fillText(new Date(b.start).toLocaleDateString(undefined,{month:"short",day:"numeric"}),p.l+i*slot+slot/2,h-15)})}
function showTip(ev){if(!S.buckets.length)return;const r=E.chart.getBoundingClientRect(),x=(ev.touches?.[0]?.clientX??ev.clientX)-r.left,y=(ev.touches?.[0]?.clientY??ev.clientY)-r.top,pL=45,pR=18,slot=(r.width-pL-pR)/S.buckets.length,idx=Math.max(0,Math.min(S.buckets.length-1,Math.floor((x-pL)/slot))),b=S.buckets[idx];if(!b)return;const reps=[...b.repos].sort((a,c)=>c[1].count-a[1].count).slice(0,6);E.tip.innerHTML=`<h3>${new Date(b.start).toLocaleDateString()}${b.end-b.start>DAY?" - "+new Date(b.end-1).toLocaleDateString():""}</h3><div class="tipgrid"><span>Commits</span><span>${fmt(b.commits.length)}</span><span>Repositories</span><span>${fmt(b.repos.size)}</span><span>Lines added</span><span>${b.known?"+"+fmt(b.add):"not loaded"}</span><span>Lines removed</span><span>${b.known?"-"+fmt(b.del):"not loaded"}</span><span>Net change</span><span>${b.known?(b.add-b.del>=0?"+":"")+fmt(b.add-b.del):"not loaded"}</span><span>Files changed</span><span>${b.known?fmt(b.files):"not loaded"}</span></div><div class="repos">${reps.map(([n,v])=>`<div class="reporow"><b>${esc(n)}</b><span>${v.count} commit${v.count===1?"":"s"}${v.add||v.del?` / +${fmt(v.add)} -${fmt(v.del)}`:""}</span></div>`).join("")}</div>`;E.tip.classList.add("show");const tw=E.tip.offsetWidth,th=E.tip.offsetHeight,m=8;let left=x+16;if(left+tw>r.width-m)left=x-tw-16;left=Math.max(m,Math.min(r.width-tw-m,left));let top=y-th-16;if(top<m)top=y+18;if(top+th>r.height-m)top=r.height-th-m;E.tip.style.left=left+"px";E.tip.style.top=Math.max(m,top)+"px"}
async function loadStats(){if(S.loadingStats)return;const list=filtered().filter(c=>!c.stats),limit=Math.min(40,list.length);if(!limit)return toast("Line details are already loaded for this range.");S.loadingStats=true;E.stats.disabled=true;let done=0;const queue=list.slice(0,limit);busy(true,`Loading exact line details 0 of ${limit}...`,0);async function worker(){while(queue.length){const c=queue.shift();try{const d=await api(`/repos/${encodeURIComponent(repoOf(c).split("/")[0])}/${encodeURIComponent(repoOf(c).split("/")[1])}/commits/${encodeURIComponent(c.sha)}`);c.stats={additions:d.stats?.additions||0,deletions:d.stats?.deletions||0,total_files:d.files?.length||0}}catch(e){}done++;busy(true,`Loading exact line details ${done} of ${limit}...`,done/limit*100)}}await Promise.all(Array.from({length:Math.min(4,limit)},worker));busy(false);S.loadingStats=false;E.stats.disabled=false;render();toast(`Loaded exact line details for ${done} commits.`)}
E.form.addEventListener("submit",e=>{e.preventDefault();loadUser(E.user.value)});document.querySelectorAll(".ex").forEach(b=>b.onclick=()=>{E.user.value=b.dataset.u;loadUser(b.dataset.u)});$("ranges").onclick=e=>{const b=e.target.closest("[data-range]");if(b){S.range=b.dataset.range;render