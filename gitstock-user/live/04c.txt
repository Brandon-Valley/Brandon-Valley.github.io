ion goHome(){E.app.style.display="none";E.home.style.display="block";E.tip.classList.remove("show");history.replaceState(null,"",location.pathname+location.search);window.scrollTo(0,0)}
function startDate(){const now=Date.now(),d={"30D":30,"90D":90,"1Y":365}[S.range];return d?new Date(now-d*DAY):new Date(Math.min(...S.commits.map(c=>+dateOf(c)),now))}
function filtered(){const start=+startDate();return S.commits.filter(c=>+dateOf(c)>=start)}
function makeBuckets(){const list=filtered().slice().sort((a,b)=>dateOf(a)-dateOf(b));if(!list.length){S.buckets=[];return}const start=+startDate(),end=Date.now(),span=Math.max(DAY,end-start),step=Math.max(DAY,Math.ceil(span/42/DAY)*DAY),map=new Map;for(const c of list){const t=+dateOf(c),idx=Math.floor((t-start)/step),key=start+idx*step;if(!map.has(key))map.set(key,{start:key,end:key+step,commits:[],repos:new Map,add:0,del:0,files:0,known:0});const b=map.get(key),r=repoOf(c);b.commits.push(c);if(!b.repos.has(r))b.repos.set(r,{count:0,add:0,del:0});const rr=b.repos.get(r);rr.count++;if(c.stats){b.add+=c.stats.additions||0;b.del+=c.stats.deletions