}`);let all=[];for(let p=1;p<=10;p++){busy(true,`Loading commit page ${p} of up to 10...`,10+p*7);const d=await api(`/search/commits?q=${encodeURIComponent(`author:${u}`)}&sort=committer-date&order=desc&per_page=100&page=${p}`);const items=(d.items||[]).filter(x=>!x.repository?.private);all.push(...items);if(items.length<100)break;await new Promise(r=>setTimeout(r,250))}const seen=new Set;S.commits=all.filter(c=>{const k=repoOf(c)+":"+c.sha;if(seen.has(k))return false;seen.add(k);return true}).sort((a,b)=>dateOf(b)-dateOf(a));showApp();render();location.hash=encodeURIComponent(u);busy(false);if(!S.commits.length)toast("No public commits were found.",true)}catch(e){busy(false);toast(e.status===403?"GitHub public API limit reached. Wait a while and retry.":e.message||"GitHub request failed.",true)}}
function showApp(){E.home.style.display="none";E.app.style.display="block";E.avatar.src=S.profile.avatar_url;E.name.textContent=S.profile.name||S.profile.login;E.meta.textContent=`${fmt(S.profile.public_repos)} public repositories / ${fmt(S.profile.followers)} followers`;E.bio.textContent=S.profile.bio||"Public commit activity across GitHub.";E.open.href=S.profile.html_url;window.scrollTo(0,0)}
funct